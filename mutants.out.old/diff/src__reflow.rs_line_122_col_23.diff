--- src/reflow.rs
+++ replace -= with += in piece_open_paren
@@ -114,17 +114,17 @@
 
 /// Index of the `(` matching the `)` at `close`, scanning the piece list backward.
 fn piece_open_paren(pieces: &[Piece], close: usize) -> Option<usize> {
     let mut depth = 0i32;
     for j in (0..=close).rev() {
         match pieces[j].1.text {
             ")" => depth += 1,
             "(" => {
-                depth -= 1;
+                depth += /* ~ changed by cargo-mutants ~ */ 1;
                 if depth == 0 {
                     return Some(j);
                 }
             }
             _ => {}
         }
     }
     None
