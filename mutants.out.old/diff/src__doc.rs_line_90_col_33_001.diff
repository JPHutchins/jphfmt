--- src/doc.rs
+++ replace * with / in render
@@ -82,17 +82,17 @@
                         col += 1;
                     }
                 }
                 Mode::Break => {
                     out.push('\n');
                     for _ in 0..level {
                         out.push('\t');
                     }
-                    col = level * TAB_WIDTH;
+                    col = level / /* ~ changed by cargo-mutants ~ */ TAB_WIDTH;
                 }
             },
             Doc::Group(inner) => {
                 let mode = if fits(width.saturating_sub(col), inner, &stack) {
                     Mode::Flat
                 } else {
                     Mode::Break
                 };
