--- src/reflow.rs
+++ replace + with * in build_element_doc
@@ -1011,17 +1011,17 @@
             if pending_space && !text.is_empty() {
                 text.push(' ');
             }
             pending_space = false;
             if !text.is_empty() {
                 parts.push(Doc::Text(std::mem::take(&mut text)));
             }
             parts.push(build_brace_doc(&toks[j + 1..close], false));
-            j = close + 1;
+            j = close * /* ~ changed by cargo-mutants ~ */ 1;
         } else {
             if pending_space {
                 text.push(' ');
                 pending_space = false;
             }
             text.push_str(t.text);
             j += 1;
         }
