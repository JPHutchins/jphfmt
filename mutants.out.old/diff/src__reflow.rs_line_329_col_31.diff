--- src/reflow.rs
+++ replace == with != in retab
@@ -321,17 +321,17 @@
     let mut at_line_start = true;
     for t in tokenize(s) {
         let pure_indent =
             t.kind == TokenKind::Whitespace && t.text.bytes().all(|b| b == b' ' || b == b'\t');
         if at_line_start && pure_indent {
             let cols: usize = t
                 .text
                 .chars()
-                .map(|c| if c == '\t' { TAB_WIDTH } else { 1 })
+                .map(|c| if c != /* ~ changed by cargo-mutants ~ */ '\t' { TAB_WIDTH } else { 1 })
                 .sum();
             for _ in 0..cols / TAB_WIDTH {
                 out.push('\t');
             }
             for _ in 0..cols % TAB_WIDTH {
                 out.push(' ');
             }
         } else {
