--- src/reflow.rs
+++ replace += with -= in collapse_blank_lines
@@ -57,17 +57,17 @@
     for t in tokenize(s) {
         match t.kind {
             TokenKind::Newline => {
                 if has_content {
                     out.push_str(&line);
                     out.push('\n');
                     blank_run = 0;
                 } else {
-                    blank_run += 1;
+                    blank_run -= /* ~ changed by cargo-mutants ~ */ 1;
                     if blank_run <= 1 {
                         out.push('\n');
                     }
                 }
                 line.clear();
                 has_content = false;
             }
             TokenKind::Whitespace => line.push_str(t.text),
