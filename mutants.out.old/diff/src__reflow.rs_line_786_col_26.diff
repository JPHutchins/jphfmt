--- src/reflow.rs
+++ replace == with != in matching
@@ -778,17 +778,17 @@
 fn matching(toks: &[Token], open: usize, lhs: &str, rhs: &str) -> Option<usize> {
     if toks.get(open).map(|t| t.text) != Some(lhs) {
         return None;
     }
     let mut depth = 0usize;
     for (j, t) in toks.iter().enumerate().skip(open) {
         if t.text == lhs {
             depth += 1;
-        } else if t.text == rhs {
+        } else if t.text != /* ~ changed by cargo-mutants ~ */ rhs {
             depth -= 1;
             if depth == 0 {
                 return Some(j);
             }
         }
     }
     None
 }
