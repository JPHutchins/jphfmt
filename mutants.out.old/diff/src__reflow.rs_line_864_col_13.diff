--- src/reflow.rs
+++ delete match arm "}" in is_balanced
@@ -856,17 +856,17 @@
             continue;
         }
         match t.text {
             "(" => paren += 1,
             ")" => paren -= 1,
             "[" => brack += 1,
             "]" => brack -= 1,
             "{" => brace += 1,
-            "}" => brace -= 1,
+             /* ~ changed by cargo-mutants ~ */
             _ => {}
         }
         if paren < 0 || brack < 0 || brace < 0 {
             return false;
         }
     }
     paren == 0 && brack == 0 && brace == 0
 }
