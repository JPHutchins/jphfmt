--- src/reflow.rs
+++ replace < with <= in build_element_doc
@@ -992,17 +992,17 @@
 
 /// Build one `{}` element: collapsed text, with any nested `{}` rendered as its own (tight) list so
 /// it collapses or explodes on its own.
 fn build_element_doc(toks: &[Token]) -> Doc {
     let mut parts: Vec<Doc> = Vec::new();
     let mut text = String::new();
     let mut pending_space = false;
     let mut j = 0usize;
-    while j < toks.len() {
+    while j <= /* ~ changed by cargo-mutants ~ */ toks.len() {
         let t = toks[j];
         if is_trivia(&t) {
             if !text.is_empty() || !parts.is_empty() {
                 pending_space = true;
             }
             j += 1;
         } else if t.kind == TokenKind::Punct
             && t.text == "{"
