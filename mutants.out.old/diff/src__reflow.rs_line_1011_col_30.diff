--- src/reflow.rs
+++ replace && with || in build_element_doc
@@ -1003,17 +1003,17 @@
             if !text.is_empty() || !parts.is_empty() {
                 pending_space = true;
             }
             j += 1;
         } else if t.kind == TokenKind::Punct
             && t.text == "{"
             && let Some(close) = match_brace(toks, j)
         {
-            if pending_space && !text.is_empty() {
+            if pending_space || /* ~ changed by cargo-mutants ~ */ !text.is_empty() {
                 text.push(' ');
             }
             pending_space = false;
             if !text.is_empty() {
                 parts.push(Doc::Text(std::mem::take(&mut text)));
             }
             parts.push(build_brace_doc(&toks[j + 1..close], false));
             j = close + 1;
