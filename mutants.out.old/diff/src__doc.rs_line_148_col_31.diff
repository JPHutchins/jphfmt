--- src/doc.rs
+++ replace -= with += in fits
@@ -140,17 +140,17 @@
             }
             Doc::Nest(inner) => work.push((mode, inner)),
             Doc::Group(inner) => work.push((Mode::Flat, inner)),
             Doc::Line => match mode {
                 Mode::Flat => {
                     if remaining == 0 {
                         return false;
                     }
-                    remaining -= 1;
+                    remaining += /* ~ changed by cargo-mutants ~ */ 1;
                 }
                 Mode::Break => return true,
             },
             Doc::SoftLine => {
                 if mode == Mode::Break {
                     return true;
                 }
             }
