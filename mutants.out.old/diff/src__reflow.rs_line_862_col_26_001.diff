--- src/reflow.rs
+++ replace -= with /= in is_balanced
@@ -854,17 +854,17 @@
     for t in toks {
         if t.kind != TokenKind::Punct {
             continue;
         }
         match t.text {
             "(" => paren += 1,
             ")" => paren -= 1,
             "[" => brack += 1,
-            "]" => brack -= 1,
+            "]" => brack /= /* ~ changed by cargo-mutants ~ */ 1,
             "{" => brace += 1,
             "}" => brace -= 1,
             _ => {}
         }
         if paren < 0 || brack < 0 || brace < 0 {
             return false;
         }
     }
