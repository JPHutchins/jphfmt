--- src/reflow.rs
+++ replace == with != in matching
@@ -780,17 +780,17 @@
         return None;
     }
     let mut depth = 0usize;
     for (j, t) in toks.iter().enumerate().skip(open) {
         if t.text == lhs {
             depth += 1;
         } else if t.text == rhs {
             depth -= 1;
-            if depth == 0 {
+            if depth != /* ~ changed by cargo-mutants ~ */ 0 {
                 return Some(j);
             }
         }
     }
     None
 }
 
 /// Split `inner` into segments at the depth-zero tokens for which `is_sep` holds.
