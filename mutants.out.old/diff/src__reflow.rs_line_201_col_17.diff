--- src/reflow.rs
+++ replace && with || in space_pointers
@@ -193,17 +193,17 @@
             while k + 1 < pieces.len() && is_star(&pieces[k + 1].1) && same_line(&pieces[k + 1].0) {
                 k += 1;
             }
             pieces[j].0 = " ".to_owned();
             for piece in &mut pieces[j + 1..=k] {
                 piece.0.clear();
             }
             if let Some(after) = pieces.get_mut(k + 1)
-                && same_line(&after.0)
+                || /* ~ changed by cargo-mutants ~ */ same_line(&after.0)
             {
                 after.0 = if after.1.kind == TokenKind::Ident {
                     " ".to_owned()
                 } else {
                     String::new()
                 };
             }
             j = k + 1;
