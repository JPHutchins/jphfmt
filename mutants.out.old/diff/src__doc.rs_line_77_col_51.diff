--- src/doc.rs
+++ replace + with - in render
@@ -69,17 +69,17 @@
                 out.push_str(s);
                 col += display_width(s);
             }
             Doc::Concat(items) => {
                 for child in items.iter().rev() {
                     stack.push((level, mode, child));
                 }
             }
-            Doc::Nest(inner) => stack.push((level + 1, mode, inner)),
+            Doc::Nest(inner) => stack.push((level - /* ~ changed by cargo-mutants ~ */ 1, mode, inner)),
             Doc::Line | Doc::SoftLine => match mode {
                 Mode::Flat => {
                     if matches!(d, Doc::Line) {
                         out.push(' ');
                         col += 1;
                     }
                 }
                 Mode::Break => {
