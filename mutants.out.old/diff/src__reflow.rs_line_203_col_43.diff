--- src/reflow.rs
+++ replace == with != in space_pointers
@@ -195,17 +195,17 @@
             }
             pieces[j].0 = " ".to_owned();
             for piece in &mut pieces[j + 1..=k] {
                 piece.0.clear();
             }
             if let Some(after) = pieces.get_mut(k + 1)
                 && same_line(&after.0)
             {
-                after.0 = if after.1.kind == TokenKind::Ident {
+                after.0 = if after.1.kind != /* ~ changed by cargo-mutants ~ */ TokenKind::Ident {
                     " ".to_owned()
                 } else {
                     String::new()
                 };
             }
             j = k + 1;
             continue;
         }
