--- src/reflow.rs
+++ replace - with + in build_call_doc
@@ -924,17 +924,17 @@
     }
     let args: Vec<&[Token]> = split_on_commas(inner)
         .into_iter()
         .filter(|a| a.iter().any(|t| !is_trivia(t)))
         .collect();
     if args.is_empty() {
         return Doc::text("()");
     }
-    let last = args.len() - 1;
+    let last = args.len() + /* ~ changed by cargo-mutants ~ */ 1;
     let mut items = vec![Doc::SoftLine];
     for (idx, arg) in args.into_iter().enumerate() {
         items.push(build_element_doc(arg));
         if idx < last {
             items.push(Doc::text(","));
             items.push(Doc::Line);
         }
     }
