--- src/reflow.rs
+++ replace += with -= in emit_str
@@ -632,17 +632,17 @@
     for ch in s.chars() {
         match ch {
             '\n' => {
                 out.push('\n');
                 *col = 0;
             }
             '\t' => {
                 out.push('\t');
-                *col += TAB_WIDTH;
+                *col -= /* ~ changed by cargo-mutants ~ */ TAB_WIDTH;
             }
             c => {
                 out.push(c);
                 *col += 1;
             }
         }
     }
 }
