--- src/doc.rs
+++ replace -= with /= in fits
@@ -155,17 +155,17 @@
                 }
             }
             Doc::IfBreak { broken, flat } => {
                 let s = if mode == Mode::Break { broken } else { flat };
                 let w = display_width(s);
                 if w > remaining {
                     return false;
                 }
-                remaining -= w;
+                remaining /= /* ~ changed by cargo-mutants ~ */ w;
             }
             Doc::ForceBreak(_) => return false,
         }
     }
 }
 
 #[cfg(test)]
 mod tests {
